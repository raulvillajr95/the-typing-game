const mainWord = document.querySelector('.main-word')

const words = ['October', 'Bite', 'Earthquake', 'Inflate', 'Coma', 'Sausage', 'Therapist', 'Satisfaction', 'Lack', 'Necklace', 'Folk', 'Regard']

// Preset functions
function randomNum(b) {
  return Math.floor(Math.random() * b) + 1;
}